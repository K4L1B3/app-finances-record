'use client';
import {useState} from 'react';import {Wallet,ArrowRight,LockKeyhole,LoaderCircle} from 'lucide-react';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget);try{const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:data.get('username'),password:data.get('password')})});const body=await res.json();if(!res.ok)throw Error(body.error);window.location.reload();}catch(e){setError(e instanceof Error?e.message:'Não foi possível conectar.');setBusy(false);}}
return <main className="login-page"><div className="login-brand"><span className="brand-icon"><Wallet size={25}/></span> meu financeiro</div><section className="login-card"><span className="eyebrow"><LockKeyhole size={16}/> SEU ESPAÇO</span><h1>Seu dinheiro.<br/><span>Mais clareza.</span></h1><p>Acesse suas metas, gastos e reservas.</p><form onSubmit={submit}><label>Usuário<input name="username" autoComplete="username" required autoFocus maxLength={100}/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required maxLength={200}/></label>{error&&<div role="alert" className="error-banner">{error}</div>}<button className="button primary" disabled={busy}>{busy?<LoaderCircle className="spin" size={18}/>:<>Entrar <ArrowRight size={18}/></>}</button></form></section><div className="login-foot">Planejar. Acompanhar. Construir sua reserva.</div></main>;
}
