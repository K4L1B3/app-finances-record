# Gere .env com: node scripts/setup.mjs
# .env não deve ser publicado ou incluído em backups compartilhados.
APP_URL=http://localhost:3000
APP_DOMAIN=localhost
APP_PORT=3000
AUTH_USERNAME=luiz
AUTH_PASSWORD_HASH=GERADO_PELO_SETUP
SESSION_SECRET=GERADO_PELO_SETUP
DATABASE_PATH=./data/financeiro.sqlite
